import { asc, desc, isNull } from "drizzle-orm";

import { SubmitButton } from "@/components/admin/submit-button";
import { deleteHomepageItem, deleteHomepageSection, saveHomepageItem, saveHomepageSection } from "@/src/admin/operations-actions";
import { requireAdminUser } from "@/src/admin/permissions";
import { formatDateTimeLocal } from "@/src/admin/shared";
import { getDb } from "@/src/db";
import { articles, homepageItems, homepageSectionKindValues, homepageSections, media } from "@/src/db/schema";

export default async function HomepageAdminPage() {
  await requireAdminUser("homepage:manage"); const db = await getDb();
  const [sections, items, articleRows, mediaRows] = await Promise.all([db.query.homepageSections.findMany({ orderBy: [asc(homepageSections.position)] }), db.query.homepageItems.findMany({ orderBy: [asc(homepageItems.position)] }), db.query.articles.findMany({ columns: { id: true, title: true }, where: isNull(articles.deletedAt), orderBy: [desc(articles.updatedAt)], limit: 200 }), db.query.media.findMany({ columns: { id: true, title: true, publicUrl: true }, where: isNull(media.deletedAt), orderBy: [desc(media.createdAt)], limit: 200 })]);
  const articleName = new Map(articleRows.map((item) => [item.id, item.title]));
  return <><header className="admin-page-header"><div><span className="admin-eyebrow">Front page</span><h1>Homepage curation</h1><p>Control section order, story placement and publishing windows.</p></div></header><section className="admin-card admin-form-section"><div className="admin-section-heading"><div><span className="admin-eyebrow">Layout</span><h2>Create section</h2></div></div><form action={saveHomepageSection.bind(null, null)} className="admin-form-grid four"><label>Title<input name="title" required /></label><label>Key<input name="key" placeholder="Generated from title" /></label><label>Kind<select name="kind">{homepageSectionKindValues.map((value) => <option key={value}>{value}</option>)}</select></label><label>Position<input name="position" type="number" min="1" defaultValue={sections.length + 1} /></label><SubmitButton>Create section</SubmitButton></form></section><div className="admin-homepage-stack">{sections.map((section) => { const sectionItems = items.filter((item) => item.sectionId === section.id); return <section className="admin-card admin-homepage-section" key={section.id}><div className="admin-homepage-section-head"><form action={saveHomepageSection.bind(null, section.id)} className="admin-form-grid four"><label>Title<input name="title" defaultValue={section.title} /></label><label>Key<input name="key" defaultValue={section.key} /></label><label>Kind<select name="kind" defaultValue={section.kind}>{homepageSectionKindValues.map((value) => <option key={value}>{value}</option>)}</select></label><label>Position<input name="position" type="number" min="1" defaultValue={section.position} /></label><SubmitButton>Save section</SubmitButton></form><form action={deleteHomepageSection.bind(null, section.id)}><SubmitButton danger>Delete section</SubmitButton></form></div><div className="admin-placement-list">{sectionItems.map((item) => <article key={item.id}><form action={saveHomepageItem.bind(null, item.id)} className="admin-form-grid placement"><input type="hidden" name="sectionId" value={section.id} /><label>Article<select name="articleId" defaultValue={item.articleId ?? ""}><option value="">No article</option>{articleRows.map((article) => <option key={article.id} value={article.id}>{article.title}</option>)}</select></label><label>Media<select name="mediaId" defaultValue={item.mediaId ?? ""}><option value="">Use article media</option>{mediaRows.map((asset) => <option key={asset.id} value={asset.id}>{asset.title ?? asset.publicUrl}</option>)}</select></label><label>Position<input name="position" type="number" min="1" defaultValue={item.position} /></label><label>Title override<input name="titleOverride" defaultValue={item.titleOverride ?? ""} /></label><label>Dek override<input name="dekOverride" defaultValue={item.dekOverride ?? ""} /></label><label>Starts<input name="startsAt" type="datetime-local" defaultValue={formatDateTimeLocal(item.startsAt)} /></label><label>Ends<input name="endsAt" type="datetime-local" defaultValue={formatDateTimeLocal(item.endsAt)} /></label><SubmitButton>Save placement</SubmitButton></form><div className="admin-placement-title">{item.articleId ? articleName.get(item.articleId) : "Media placement"}</div><form action={deleteHomepageItem.bind(null, item.id)}><SubmitButton danger>Remove</SubmitButton></form></article>)}</div><form action={saveHomepageItem.bind(null, null)} className="admin-form-grid placement admin-new-placement"><input type="hidden" name="sectionId" value={section.id} /><label>Article<select name="articleId"><option value="">No article</option>{articleRows.map((article) => <option key={article.id} value={article.id}>{article.title}</option>)}</select></label><label>Media<select name="mediaId"><option value="">Use article media</option>{mediaRows.map((asset) => <option key={asset.id} value={asset.id}>{asset.title ?? asset.publicUrl}</option>)}</select></label><label>Position<input name="position" type="number" min="1" defaultValue={sectionItems.length + 1} /></label><label>Title override<input name="titleOverride" /></label><label>Dek override<input name="dekOverride" /></label><label>Starts<input name="startsAt" type="datetime-local" /></label><label>Ends<input name="endsAt" type="datetime-local" /></label><SubmitButton>Add placement</SubmitButton></form></section>; })}</div></>;
}
